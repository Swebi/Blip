import IDE from "@/components/Editor";
import React from "react";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";

const Home = () => {
  return (
    <div className="w-full h-screen flex flex-col justify-center items-center pt-5 pb-10  bg-[#131415]">
      Home
    </div>
  );
};

export default Home;
