import { Skeleton } from "@/components/ui/skeleton";

import React from "react";

const SignInLoader = () => {
  return <Skeleton className="h-[485px] w-[400px] border dark" />;
};

export default SignInLoader;
